# ASCII Align Env

An RL environment for training and evaluating LLMs on generating ASCII diagrams.

## Structure

- `environments/ascii_align/`: environment, rubric, and tests
- `rollout-dashboard/`: RL Studio web UI for rollout inspection and reward visualization
